/**
 * Dashboard endpoint currently has no query/body params.
 * Validator kept for architecture consistency and future filters.
 */

export const getDashboardValid = [];

export default {
    getDashboardValid
};
