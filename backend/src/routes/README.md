# Routes Directory

Contains the API route definitions. This layer typically binds HTTP endpoints (e.g., GET, POST, PUT, DELETE) to their respective controller functions located in the `modules/` directory.
