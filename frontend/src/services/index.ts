/**
 * Application services layer.
 * All HTTP calls must live in services (or feature api modules).
 * Do not call apiClient from components.
 *
 * TODO: Add domain services (auth, topics, tests, etc.).
 */
export {};
